# memory — @batthewz/response-ui-renderer

Durable notes for anyone (human or agent) working in this package. Principles and
traps only: no TODOs, no line numbers, no file-by-file inventory.

| File | What it holds |
| --- | --- |
| [extending-the-registry.md](extending-the-registry.md) | Why registering a component was never the whole extension story, why the per-component knowledge is a computed view over the tables rather than a second copy of them and what that leaves ungated, why a merge API must be per-field and never global, what actually decides an issue's severity, why a name suggestion has to treat a transposition as one edit, why the doc generator is split into a deriver and a shipped renderer rather than shipped whole, why generating a document from a template that *is* that document makes half the checks unfalsifiable and what to do with a region no input can change, the two ways a fixture ends up unable to see the guard it is named after, why a generated table a scope empties has to say so, why a scoped profile has to name the components its examples still author, which of the two kinds of absence a scope may filter and which is settled as never filtered, how a scope must treat a compound part and a name it does not recognise, and why a limit calibrated for breadth has to become an option |
| [driving-a-component-library-from-json.md](driving-a-component-library-from-json.md) | The recurring shapes that defeat a JSON renderer, which of them can be fixed at all, why a coercion keyed on a prop's name must run on the resolved side of a reference, why a per-node error boundary must never remember, and which props may key a child and which are data that two siblings may honestly repeat |
| [authoring-documents.md](authoring-documents.md) | Making a document look like a product: scoped theme overrides need the document to paint its own canvas, component defaults that fight polish, and how to verify visually |
| [composing-several-documents.md](composing-several-documents.md) | What breaks only when a page mounts more than one renderer, why the DOM id namespace cannot be fixed from the host side, how to choose the direction of a default when a rule keys on an overloaded prop name, and what a scoped namespace still cannot reach |
| [testing.md](testing.md) | How to run the suite, what jsdom lacks — including the top layer and `inert`, which makes overlay coverage look wider than it is — where a deliberately thrown error is silenced and why that silence must stay scoped to the tests that expect it, and how to avoid writing a check that cannot fail |
| [invariants.md](invariants.md) | Which rules are enforced by tests rather than remembered, why the two validators agree only on conformance, and why the design system's `cn()` rule must not be applied here |
| [gates.md](gates.md) | Every hand-maintained list in this package and the test that keeps it honest, plus the source-hygiene gates, what upgrading the peer library actually requires, why a table of places-to-look-for-danger needs a gate that catches an omission rather than a rename and why keying it on the value instead is the wrong fix, why the scheme test is an allowlist, why an upstream behaviour change can leave every gate green while only the prose needs to move, why the bump failure that looks most like a regression is usually a count a test restates rather than derives, why two artifacts from one generator must be checked against each other rather than themselves, which facts a prop's type cannot carry at all, and why the generated contracts describe the installed library rather than the sibling checkout — which fixes the release order between the two packages |
| [payload-size.md](payload-size.md) | How much smaller a document really is than the equivalent markup, where the saving comes from, and the two ways this measurement flatters itself |
| [building-a-document-by-hand.md](building-a-document-by-hand.md) | Why an editor for this format may not know anything about components, why an insertion template has to be resolved where the document that holds its data still is and why guessing from a prop's type instead breaks quietly, which children must never be lifted out of the document they came from, why the first occurrence of a component is the wrong template and what to do about the ones that only ever wrap a page, how a canvas can be the real render without changing what the renderer emits, why a drop is two questions rather than one, the half of a drag that compiles and renders and is still missing, and what a document opened into it must survive, which repeated prop a drop may rename and which it must leave alone, why the chrome can wear a library component without a specificity fight — plus the state-toggling properties that same cascade fact makes it dangerous to restate — why a palette that lists every addressable name is mostly in its own way and where a compound part belongs instead, which direction to search from when offering one and which word to take off its face, why the components a palette leads with have to be counted rather than chosen and the two things that keep the count honest, why browsing and searching must share one arrangement, the fact the panel had already derived and only ever told a screen reader, why a derivation keyed on a component's own name drops its compound parts and why an absent derivation and a genuine absence read identically to whoever is looking at the panel, why an invariant that has only ever run over a population carrying no data is true and empty and what a failure right after enriching one usually means, why matching a prop's type by its exact spelling misses the nullable spelling of it and where a control may consult a prop's *name* at all, and why whether a drop can succeed is derivable from the contract and the template rather than listable — plus what has to be said out loud when one is withheld |
| [dev-harness.md](dev-harness.md) | Why the published site's prose pages are documents rather than a docs feature, why a frame owns its chrome and pages draw none, why that chrome must be built from tokens, what a control or a number in the chrome claims by accident, why a help panel has to be a column, the device-preview trap, and the routes the visual workflow depends on |
